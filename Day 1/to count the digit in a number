#include <stdio.h>
int main() {
    int n,c=0;
    printf("enter the number");
    scanf("%d",&n);
    while(n>0){
        c++;
        n=n/10;
    }
    printf("the total no of digit are %d\n",c);
    return 0;
}
